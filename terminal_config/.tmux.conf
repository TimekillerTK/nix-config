# MacOS Config

set -g default-terminal "xterm-256color"
set -g mouse on

# Start windows and panes (tabs) at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Bind keys for Home/End beginning of line
bind-key -n Home send Escape "OH"
bind-key -n End send Escape "OF"